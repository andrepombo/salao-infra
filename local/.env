# Domain Traefik will route for (matches APP_DOMAIN in labels)
APP_DOMAIN=localhost

# Email used by Let's Encrypt (can be any valid email)
ACME_EMAIL=you@example.com

# Backend DB settings (Postgres service defined in docker-compose.yml)
DB_HOST=db
DB_NAME=salao
DB_USER=salao
DB_PASSWORD=changeme
DB_PORT=5432

# Media bucket placeholder for local use
MEDIA_BUCKET=local-media-bucket
